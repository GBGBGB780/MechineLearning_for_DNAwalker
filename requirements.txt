# Core deep-learning framework
torch>=2.5.1

# Scientific computing and data processing
# NumPy 2.x is required because reviewed scaler files use its serialization.
numpy>=2.0.2
# The project uses stable dataframe/Excel APIs; cap the next breaking major.
pandas>=2.1,<4
scipy>=1.17.1
scikit-learn>=1.7.2

# Plotting for verification, evaluation, and evidence scripts
matplotlib>=3.7.1

# Excel workbook support for pandas
openpyxl>=3.1.5

# MATLAB v7.3 (HDF5) to NPZ conversion
h5py>=3.11.0

# Dev/test dependencies for the validation-experiments suite.
pytest>=9.0.3
hypothesis>=6.155.1
pyflakes>=3.4.0

# Reproducible installation:
#   uv pip install --torch-backend cpu -r requirements-lock.txt
# ``constraints-tested-py312.txt`` records the exact environment used for the
# 2026-06-26 local audit; the generated CPU lock is portable across OSes.

# Optional: only used by notebook dimensionality-reduction visualization.
# It is not a core dependency because it pulls in numba/llvmlite.
#   umap-learn>=0.5.9
